(function (window) {
  window.__env = window.__env || {};

  window.__env.apiBaseUrl = "${API_BASE_URL}";
  window.__env.apiMultiBackend = ${ADMINIO_MULTI_BACKEND};
  window.__env.apiBackends = ${ADMINIO_BACKENDS};

}(this));
